services:
  service-a:
    build:
      context: ./service-a
    ports:
      - "5000:5000"

  service-b:
    build:
      context: ./service-b
    depends_on:
      - service-a
    environment:
      SERVICE_A_URL: http://service-a:5000